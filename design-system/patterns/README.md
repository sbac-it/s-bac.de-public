# Patterns

`layout.css` enthält wiederverwendbare Layout- und Seitenmuster. Header, Footer
und Navigation werden als versionierte Quellen unter `src/` geführt und durch
den statischen Generator in den auslieferbaren Stand übernommen.
